import { createLightTheme, lightThemePrimitives } from 'baseui';

const SynkTheme = createLightTheme({
  ...lightThemePrimitives,
  primaryFontFamily: '"Poppins", sans-serif',
});

export default SynkTheme;
