import { createDarkTheme, darkThemePrimitives } from 'baseui';

const SynkTheme = createDarkTheme({
  ...darkThemePrimitives,
  primaryFontFamily: '"Poppins", sans-serif',
});

export default SynkTheme;
