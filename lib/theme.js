import { createTheme, lightThemePrimitives } from 'baseui';

const SynkTheme = createTheme({
  ...lightThemePrimitives,
  primaryFontFamily: '"Poppins", sans-serif',
  primary: '#222222',
  accent: '#F23D4F',
});

export default SynkTheme;
